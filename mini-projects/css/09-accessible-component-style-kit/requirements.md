# CSS-09: acceptance requirements

[Full brief](README.md) is authoritative for scope.

| Requirement | Evidence to submit |
| --- | --- |
| FR-1: Create button, input, card and alert styles with focus states, contrast checks and reduced-motion support | A repeatable input/action and actual observed result |
| Acceptance oracle | Keyboard focus is visible on every interactive component |

Record fixture values, expected result, actual result and pass/fail for each row. Identify any unmet requirement explicitly. Optional extensions cannot replace a required behavior.

