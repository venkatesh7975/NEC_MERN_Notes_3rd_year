# Control flow before array helpers

Run `node dailycodes/javascript/000-control-flow/app.js` from the root. No dependencies.

Expected: attendance 15/20 gives percentage 75 and eligible true. The practice threshold is not an official attendance rule. Numbers 0–3 are classified even, odd, even, odd.

Try total=0, present greater than total and a non-integer loop bound: each must reject input. This example introduces types, operators, conditions and loops before the existing filter/map/reduce example.
